package com.miss.dialog.lemonhello.enums;

/**
 * LemonHello - 图标摆放位置枚举
 */

public enum LemonHelloIconLocation {

    LEFT(0),
    TOP(1),
    RIGHT(2);

    private int code;

    LemonHelloIconLocation(int code) {
        this.code = code;
    }

}
