<resources>

    <!-- Base application theme. -->
    <style name="AnimFade" parent="@android:style/Animation.Activity">
         <item name="android:activityOpenEnterAnimation">@anim/slide_in_from_right</item>
         <item name="android:activityOpenExitAnimation">@anim/slide_out_to_left</item>
          <item name="android:activityCloseExitAnimation">@anim/slide_out_to_right</item>
          <item name="android:activityCloseEnterAnimation">@anim/slide_in_from_left</item>
    </style>

    <style name="Splash" parent="@android:style/Animation.Activity">
         <item name="android:activityOpenEnterAnimation">@anim/modal_in</item>
         <item name="android:activityOpenExitAnimation">@anim/modal_out</item>
        <item name="android:activityCloseExitAnimation">@anim/modal_out</item>
        <item name="android:activityCloseEnterAnimation">@anim/modal_in</item>
    </style>
</resources>
